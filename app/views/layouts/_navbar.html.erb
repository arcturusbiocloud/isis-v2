<nav class="navbar navbar-white navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag("arcturus.png", width: "70", height: "66", alt: "Arcturus BioCloud"), root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li class="<%= is_active?("/#{current_user.username}") %>"><%= link_to 'My projects', username_projects_path(current_user.username) %></li>
          <li class="divisor">|</li>
        <% end %>
        <li class="<%= is_active?('/explore') %>"><%= link_to 'Explore', explore_path %></li>
        <!--
        <li class="divisor">|</li>
        <li class="<%= is_active?('/people') %>"><a href="/people">People</a></li>
        <li class="divisor">|</li>
        <li class="<%= is_active?('/learning') %>"><a href="/learning">Learning</a></li>
        -->
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <!--
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">My account <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><a href="/settings">Settings</a></li>
              <li><a href="#">Another action</a></li>
              <li class="divider"></li>
              <li><%= link_to "Log out", destroy_user_session_path %></li>
            </ul>
          </li>
          -->
          <li><%= link_to "Log out", destroy_user_session_path %></li>
        <% else %>
          <li><%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-default' %></li>
          <li><%= link_to "Sign In", new_user_session_path, class: 'btn btn-blue' %></li>
        <% end %>
      </ul>

      <% if user_signed_in? %>
        <form class="nav navbar-nav navbar-right">
          <%= link_to "New Project", new_project_path, class: "btn btn-success" %>
        </form>
      <% end %>
    </div>
  </div>
</nav>
